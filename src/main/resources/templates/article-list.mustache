<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>글 목록</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* 현재 페이지 강조 스타일 */
        .pagination .page-item.active .page-link {
            background-color: #0d6efd !important; /* Bootstrap 기본 primary 색상 */
            color: white !important;
            border-color: #0d6efd !important;
        }
    </style>

</head>
<body>
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>글 목록</h1>
        <a href="/articles/new" class="btn btn-primary">새 글 작성</a>
    </div>
    <table class="table table-striped">
        <thead>
        <tr>
            <th>번호</th>
            <th>제목</th>
            <th>내용</th>
            <th>작성자</th>
            <th>작성일</th>
            <th>액션</th>
        </tr>
        </thead>
        <tbody>
        {{#articles}}
            <tr>
                <td>{{number}}</td>
                <td>{{title}}</td>
                <td>{{content}}</td>
                <td>{{writer}}</td>
                <td>{{createdAt}}</td>
                <td>
                    <a href="/articles/{{id}}/edit" class="btn btn-warning btn-sm">수정</a>
                    <a href="/articles/{{id}}/delete" class="btn btn-danger btn-sm">삭제</a>
                </td>
            </tr>
        {{/articles}}
        </tbody>
    </table>

    <!-- Pagination -->
    <nav aria-label="Page navigation example">
        <ul class="pagination justify-content-center">
            <!-- Previous Page -->
            {{#hasPrevious}}
                <li class="page-item">
                    <a class="page-link" href="/articles?page={{previousPage}}" aria-label="Previous">
                    &laquo; 이전
                    </a>
                </li>
            {{/hasPrevious}}
            {{^hasPrevious}}
                <li class="page-item disabled">
                    <span class="page-link">&laquo; 이전</span>
                </li>
            {{/hasPrevious}}

            <!-- Page Numbers -->
            {{#pageNumbers}}
                <li class="page-item {{#isCurrent}}active{{/isCurrent}}">
                    <a class="page-link" href="/articles?page={{number}}">{{number}}</a>
                </li>
            {{/pageNumbers}}

            <!-- Next Page -->
            {{#hasNext}}
                <li class="page-item">
                    <a class="page-link" href="/articles?page={{nextPage}}" aria-label="Next">
                    다음 &raquo;
                    </a>
                </li>
            {{/hasNext}}
            {{^hasNext}}
                <li class="page-item disabled">
                    <span class="page-link">다음 &raquo;</span>
                </li>
            {{/hasNext}}
        </ul>
    </nav>


</div>
</body>
</html>

